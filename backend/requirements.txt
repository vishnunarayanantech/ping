fastapi
uvicorn[standard]
sqlalchemy
pydantic
email-validator
# bcrypt is pinned below 4.1 because passlib 1.7.4 (unmaintained) reads a
# bcrypt.__about__ attribute that 4.1+ removed, breaking password hashing.
passlib==1.7.4
bcrypt<4.1.0
pyjwt
python-dotenv
# Required by FastAPI/Starlette to parse multipart/form-data (file uploads).
python-multipart
# Avatar upload: decodes/validates the actual image bytes (never trusts the
# client's claimed Content-Type or filename extension) and does the
# square-crop/resize/re-encode — see services/avatar_service.py.
pillow
